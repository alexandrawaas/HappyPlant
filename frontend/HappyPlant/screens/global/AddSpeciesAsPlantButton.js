import { Button } from "react-native"

export default function AddSpeciesAsPlantButton({onPress}) {
    return (
        <Button
            onPress={onPress}
            title="test"
            color="grey"
        />
    )
}
